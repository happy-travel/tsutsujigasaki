{{- $appPort := .Values.container.port -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Chart.Name }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}
    spec:
      containers:
        - name: app
          image: {{ .Values.registry }}/tsutsujigasaki:{{ .Release.Namespace }}-{{ .Chart.Version }}
          ports:
            - containerPort: {{ $appPort }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ $appPort }}
          readinessProbe:
            httpGet:
              path: /health
              port: {{ $appPort }}
      imagePullSecrets:
      - name: travel-key-registry